const openContact = () => {
  const modal = document.querySelector("#contact_modal");
  displayModal(modal);
};

const closeContact = () => {
  const modal = document.querySelector("#contact_modal");
  closeModal(modal);
};
